name: tourding-prod

services:
  redis:
    image: redis:7-alpine
    container_name: tourding-prod-redis
    restart: unless-stopped
    command: redis-server --appendonly yes --port ${REDIS_PORT:-6379} --requirepass ${REDIS_PASSWORD}
    environment:
      REDIS_PORT: ${REDIS_PORT:-6379}
      REDIS_PASSWORD: ${REDIS_PASSWORD}
    volumes:
      - redis_data:/data
    expose:
      - "${REDIS_PORT:-6379}"
    networks:
      tourding-prod-net:
        ipv4_address: ${REDIS_CONTAINER_IP:-10.10.130.11}
    healthcheck:
      test: ["CMD-SHELL", "redis-cli -p \"$${REDIS_PORT}\" -a \"$${REDIS_PASSWORD}\" ping | grep PONG"]
      interval: 30s
      timeout: 3s
      retries: 3
      start_period: 10s

  app:
    image: ${DOCKER_IMAGE:-ghcr.io/REPLACE_ME/tourding}:${IMAGE_TAG:-latest}
    container_name: tourding-prod-app
    restart: unless-stopped
    depends_on:
      - redis
    environment:
      TZ: ${TZ:-Asia/Seoul}
      SERVER_PORT: 8080
      SERVER_FORWARD_HEADERS_STRATEGY: framework
      SPRING_PROFILES_ACTIVE: ${SPRING_PROFILES_ACTIVE:-prod}
      SPRING_APPLICATION_NAME: ${SPRING_APPLICATION_NAME:-tourding}
      SPRING_DATASOURCE_URL: ${MYSQL_URL}
      SPRING_DATASOURCE_USERNAME: ${MYSQL_USERNAME}
      SPRING_DATASOURCE_PASSWORD: ${MYSQL_PASSWORD}
      SPRING_DATASOURCE_DRIVER_CLASS_NAME: ${MYSQL_DRIVER:-com.mysql.cj.jdbc.Driver}
      SPRING_JPA_HIBERNATE_DDL_AUTO: ${SPRING_JPA_HIBERNATE_DDL_AUTO:-update}
      SPRING_JPA_SHOW_SQL: ${SPRING_JPA_SHOW_SQL:-false}
      SPRING_JPA_PROPERTIES_HIBERNATE_DIALECT: ${MYSQL_DIALECT:-org.hibernate.dialect.MySQL8Dialect}
      SWAGGER_SERVER_URL: ${SWAGGER_SERVER_URL}
      SWAGGER_USER: ${SWAGGER_USER}
      SWAGGER_PASS: ${SWAGGER_PASS}
      NAVER_CLIENT_ID: ${NAVER_CLIENT_ID}
      NAVER_CLIENT_SECRET: ${NAVER_CLIENT_SECRET}
      TOUR_CLIENT_SERVICEKEY: ${TOUR_CLIENT_SERVICEKEY}
      KAKAO_CLIENT_KAKAOAK: ${KAKAO_CLIENT_KAKAOAK}
      OPEN_ROUTE_SERVICE_KEY: ${OPEN_ROUTE_SERVICE_KEY}
      OPEN_ROUTE_SERVICE_BASE_URL: ${OPEN_ROUTE_SERVICE_BASE_URL:-https://api.openrouteservice.org}
      OPEN_ROUTE_SERVICE_CACHE_TTL_MINUTES: ${OPEN_ROUTE_SERVICE_CACHE_TTL_MINUTES:-10}
      REDIS_HOST: ${REDIS_HOST:-redis}
      REDIS_PORT: ${REDIS_PORT:-6379}
      REDIS_PASSWORD: ${REDIS_PASSWORD}
      OPEN_AI_SECRET_KEY: ${OPEN_AI_SECRET_KEY}
      OPENAI_STT_MODEL: ${OPENAI_STT_MODEL:-gpt-4o-mini-transcribe}
      OPENAI_INTENT_MODEL: ${OPENAI_INTENT_MODEL:-gpt-5-mini}
      AI_INTENT_RULE_FIRST: ${AI_INTENT_RULE_FIRST:-true}
      AI_AUDIO_MAX_SECONDS: ${AI_AUDIO_MAX_SECONDS:-8}
      AI_REQUEST_TIMEOUT_MS: ${AI_REQUEST_TIMEOUT_MS:-15000}
      AI_CANDIDATE_COUNT: ${AI_CANDIDATE_COUNT:-3}
      OPEN_WEATHER_MAP_KEY: ${OPEN_WEATHER_MAP_KEY}
      APPLE_KEY_ID: ${APPLE_KEY_ID}
      APPLE_ISS: ${APPLE_ISS}
      APPLE_BUNDLE_ID: ${APPLE_BUNDLE_ID}
      APPLE_PRIVATE_KEY: ${APPLE_PRIVATE_KEY}
      JAVA_OPTS: ${JAVA_OPTS:--Xms256m -Xmx512m}
    expose:
      - "8080"
    networks:
      tourding-prod-net:
        ipv4_address: ${APP_CONTAINER_IP:-10.10.130.10}
    healthcheck:
      test: ["CMD-SHELL", "true"]
      interval: 30s
      timeout: 3s
      retries: 3
      start_period: 60s
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

networks:
  tourding-prod-net:
    driver: bridge
    ipam:
      config:
        - subnet: ${DOCKER_NETWORK_SUBNET:-10.10.130.0/24}

volumes:
  redis_data:
